# Secure Proxy Manager - Environment Configuration
# Copy this file to .env and customize the values before starting the application

# ==============================================================================
# AUTHENTICATION CREDENTIALS
# ==============================================================================
# IMPORTANT: Change these default credentials before deploying to production!
# These credentials are used for:
# - Web UI login (http://localhost:8011)
# - API authentication
# - Backend service authentication

BASIC_AUTH_USERNAME=admin
BASIC_AUTH_PASSWORD=admin

# ==============================================================================
# SECURITY CONFIGURATION
# ==============================================================================
# Secret key for Flask session management
# Generate a new one with: python3 -c "import secrets; print(secrets.token_hex(32))"
# Leave empty to auto-generate (not recommended for production)
SECRET_KEY=

# ==============================================================================
# FLASK ENVIRONMENT
# ==============================================================================
# Set to 'development' for debugging, 'production' for deployment
FLASK_ENV=production

# ==============================================================================
# BACKEND API CONFIGURATION
# ==============================================================================
# Backend API URL (used by the Web UI to communicate with the backend)
# Default uses Docker internal networking
BACKEND_URL=http://backend:5000

# API request timeout in seconds
REQUEST_TIMEOUT=30

# Maximum number of API retry attempts
MAX_RETRIES=5

# Exponential backoff factor for retries
BACKOFF_FACTOR=1.0

# Wait time after startup before attempting backend connection (seconds)
RETRY_WAIT_AFTER_STARTUP=10

# ==============================================================================
# PROXY SERVICE CONFIGURATION
# ==============================================================================
# Proxy service hostname (Docker service name)
PROXY_HOST=proxy

# Proxy service port
PROXY_PORT=3128

# Proxy container name for management operations
PROXY_CONTAINER_NAME=secure-proxy-proxy-1

# ==============================================================================
# NOTES
# ==============================================================================
# - After editing this file, rename it to .env
# - Restart the services for changes to take effect: docker-compose restart
# - Never commit the .env file to version control (it contains sensitive data)
# - For production deployments, ensure strong passwords are used
